1. 서버 토큰 이용 방식에 대한 전반적인 흐름
 - 인증 서버 토큰, 사용자 토큰

2. 로그인 시 get   /user/{username} 사용자 ID로 조회
 - 여기서 authId가 반환되는데 이게 사용자 토큰인가?
 - 로그인할 때 인증서버를 통해 로그인하는 것이 아니라 PHR 서버를 통해 DB에 저장된 유저 정보를 조회하고 로그인 하는 것인가?

3. 헬스올에서 준 API 만으로 충분한지 아니면 키클록 REST API 참고했는지

4. AES256 이용한 방식 -> 로컬에 저장할때 이용? or 인증 서버에도 암호화하여 저장?

5. navigation 이용하는 이유?
 - 꼭 이용해야 하는가

6. API에 보면 게이트웨이 추가하는 부분이 있던데 이것은 왜 필요한가?

7. 보호자 관계 설정에서 1:m 이런 관계에 대해서 android 쪽에서 처리할 부분이 있는가?

8. 만성 설계 화면에 대한 피드백??

9. BaseActivity 사용하는 이유(listener를 따로 빼는 이유)?